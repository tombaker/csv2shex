[pytest]
addopts = -s -rf -v --showlocals --tb=short --strict --ignore-glob='_*.py'
markers =
    prefixes: @pytest.mark.prefixes
    csvshape: @pytest.mark.csvshape
    shex:     @pytest.mark.shex
filterwarnings =
    ignore::DeprecationWarning
